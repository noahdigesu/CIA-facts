export const enum QUESTION_TYPE {
    question = "question",
    answer = "answer"
}

export const enum DIRECTION {
    next= "next",
    previous = "previous"
}

export const enum STATE {
    normal = "normal", previous = "previous", current = "current"
}

export const enum TAG {
    incomplete = "incomplete",
    passed = "passed",
    failed = "failed",
    starred = "starred"
}